// Grab elements from the dom.
var character = document.querySelector(".character");
var map = document.querySelector(".map");


// character state

var x =0;
var y = 0;
var held_directions = []; // arrow keys being held down;

var speed = 1; // pixels per frame

